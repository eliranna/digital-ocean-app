import React from 'react'
import styled from "styled-components/macro"

const PaymentFailed = () => {
    return (
        <div>
            PaymentFailed!
        </div>
    )
}

export default PaymentFailed